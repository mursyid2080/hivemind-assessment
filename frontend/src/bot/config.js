import { createChatBotMessage } from 'react-chatbot-kit';

const config = {
  initialMessages: [createChatBotMessage(`Hi! I'm a chatbot for Kuala Lumpur Subway Outlets. How can I help you today?`)],
};

export default config;